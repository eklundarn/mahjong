# 🀄 Mahjong Club Manager

A web-based system for managing a mahjong club — game registration, player statistics, and rankings. Built with PHP and MySQL, designed for mobile-first use during game sessions.

## Features

- **Mobile game registration** — Register MCR mahjong games hand by hand on your phone, with automatic score calculation, self-drawn/discard tracking, and penalty support
- **Player management** — Member database with EMA numbers, roles, and permissions
- **Statistics & rankings** — Yearly rankings, head-to-head stats, player profiles, game history
- **Offline support** — Games are saved locally and queued for sync when connection returns
- **Bilingual** — Swedish and English throughout (language detection + manual switch)
- **Responsive** — Mobile-first design, works on phones, tablets, and desktops

## Requirements

- PHP 8.0 or higher
- MySQL 5.7+ or MariaDB 10.3+
- PDO MySQL extension
- Web server (Apache, Nginx, or similar)
- SMTP access for email features (optional)

## Installation

### 1. Download and upload

Download or clone this repository and upload the files to your web server's document root.

### 2. Run the installer

Open `https://your-domain.com/install.php` in your browser.

The installer will:
- Check system requirements
- Collect your database credentials, club name, and admin account details
- Create all required database tables
- Generate `config.local.php` with your settings
- Create your admin account

### 3. Delete install.php

**Important!** After installation, delete `install.php` from your server for security.

### 4. Log in

Go to `https://your-domain.com/login.php` and log in with the admin account you created.

## File structure

```
├── config.php              # Main configuration (do not edit)
├── config.local.php        # Your local settings (generated by installer, DO NOT COMMIT)
├── config.example.php      # Example config for reference
├── install.php             # Web installer (delete after use)
├── index.php               # Home page
├── login.php               # Login page
├── home.php                # Dashboard (after login)
├── mobile.php              # Mobile game registration
├── games.php               # Game list
├── ranking.php             # Player rankings
├── statistics.php          # Statistics overview
├── players.php             # Player/user management (admin)
├── administration.php      # Admin dashboard
├── configuration.php       # System configuration (mainadmin)
├── includes/
│   ├── header.php          # Page header + navigation
│   ├── footer.php          # Page footer
│   └── EmailHelper.php     # Email sending helper
├── lang/
│   ├── sv.php              # Swedish translations
│   └── en.php              # English translations
├── api/
│   └── session_heartbeat.php
├── PHPMailer/              # PHPMailer library (included)
├── images/                 # Game images (winds, flags)
├── img/                    # Site images (logo)
├── js/                     # JavaScript files
└── templates/              # Document templates
```

## Configuration

All configuration is in `config.local.php` (generated by the installer). Key settings:

| Setting | Description |
|---------|-------------|
| `DB_HOST`, `DB_NAME`, `DB_USER`, `DB_PASS` | Database connection |
| `SITE_URL` | Base URL of your site (no filename, e.g. `https://example.com/www/`) |
| `SITE_TITLE` / `SITE_TITLE_EN` | Club name in local language / English |
| `SMTP_*` | Email settings for notifications and password resets |
| `REQUIRE_PLAYER_CONFIRMATION` | If `true`, all 4 players must confirm before admin approval |
| `SESSION_TIMEOUT` | Inactivity timeout in seconds (default: 1800 = 30 min) |

## User roles

| Role | Level | Access |
|------|-------|--------|
| `reader` | 1 | View statistics and game history |
| `player` | 2 | Register games, view own statistics |
| `superuser` | 3 | Approve games, manage players |
| `admin` | 4 | All of the above + full user management |
| `mainadmin` | 5 | All of the above + system configuration |

## Scoring system

The system uses MCR (Mahjong Competition Rules) scoring:
- **Minipoints (MP)**: Raw point score from each game
- **Table points (TP)**: 4/2/1/0 based on ranking at the table (with equal sharing for ties)
- Rankings are sorted by total TP, with total MP as tiebreak

## License

MIT License — see [LICENSE](LICENSE) for details.

## Credits

Built with assistance from [Claude AI](https://claude.ai).
